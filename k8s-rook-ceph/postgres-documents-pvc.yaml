apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-documents-pvc
  namespace: otus
spec:
  storageClassName: storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
