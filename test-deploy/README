# Инструкция по запуску Docker-контейнеров

## 🐳 Быстрый старт

### 1. Получение образов

docker pull zikarimovlt/test-db:latest
docker pull zikarimovlt/test-api:latest

### 2. Создание сети

docker network create test-network

### 3. Запусп контейнеров

#### База данных

docker run -d --name test-db --network test-network \
  -e POSTGRES_PASSWORD=postgres \
  -p 5432:5432 \
  zikarimovlt/test-db:latest

#### API сервер

docker run -d --name test-api --network test-network \
  -p 8000:8000 \
  zikarimovlt/test-api:latest

### 4. Проверка работы

docker ps 

curl http://localhost:8000

docker exec -it test-api ping test-db
